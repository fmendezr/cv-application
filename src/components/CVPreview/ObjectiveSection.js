import React from "react";

const ObjectiveSection = (props) => {
    return (
        <section>   
            <h3>Objective</h3>
            <p>{props.objective}</p>
        </section>
    )
}

export default ObjectiveSection;